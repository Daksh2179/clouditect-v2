steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/clouditect-recommendation-service', '.']
    dir: 'services/recommendation-service'
  
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/clouditect-recommendation-service']
  
  # Deploy container image to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - 'run'
    - 'deploy'
    - 'clouditect-recommendation-service'
    - '--image=gcr.io/$PROJECT_ID/clouditect-recommendation-service'
    - '--region=us-central1'
    - '--platform=managed'
    - '--allow-unauthenticated'
    - '--port=4002'
    
images:
  - 'gcr.io/$PROJECT_ID/clouditect-recommendation-service'