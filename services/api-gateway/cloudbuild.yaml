steps:
  # Build the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/clouditect-api-gateway', '.']
    dir: 'services/api-gateway'
  
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/clouditect-api-gateway']
  
  # Deploy container image to Cloud Run
  - name: 'gcr.io/cloud-builders/gcloud'
    args:
    - 'run'
    - 'deploy'
    - 'clouditect-api-gateway'
    - '--image=gcr.io/$PROJECT_ID/clouditect-api-gateway'
    - '--region=us-central1'
    - '--platform=managed'
    - '--allow-unauthenticated'
    - '--port=4000'
    
images:
  - 'gcr.io/$PROJECT_ID/clouditect-api-gateway'